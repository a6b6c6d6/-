# 水贴专用脚本 · 交接文档

> 本文件写给接手这个项目的下一个 agent。你没有之前的对话上下文，请先完整读完这份文档 + `.workbuddy/memory/` 下的工作日志，再动手改代码。

## 一、项目是什么

一个 **Tampermonkey 油猴脚本**（`linux.sb-ai-reply.user.js`），用于 linux.sb 论坛（烧饼社区）的 AI 自动回帖。核心能力：

- 抓取帖子内容（三种范围：仅首楼 / 楼主全部 / 全帖），喂给大模型生成回帖
- 针对单条评论「水回应」（点评论旁的「水它」按钮，追溯 @ 关系生成回应）
- 多模态（抓正文图片喂给模型）
- 联网搜索（四阶段 Agent 编排）
- 抽奖帖自动算算术验证码
- 中转站预设切换（刚写，未测试未 commit）

用户是开发者（账号 FuyaOo0o0O / UID 7108），GitHub 用户名 `a6b6c6d6`，仓库 `lzsb-smart-water-sticker`。

## 二、当前状态（截至 2026-08-17）

| 项 | 状态 |
|---|---|
| 最新版本 | v2.6.1（已推 GitHub，远程 commit `903d815`） |
| 中转站预设功能 | 原生 select 版已实现；**已改为自定义下拉组件**（每项带重命名/删除），本地 commit `0294472`，未测试未推 |
| sendRequest 自动重试 | **已实现**（网络/超时/503 等临时失败，最多2次，退避1s→2s），本地 commit `76bf658`，未测试未推 |
| 模型可选择 | **已实现**（拉取 /models 填 datalist，按预设缓存），本地 commit `78fcca7`，未测试未推 |
| 保存设置同步预设 | **已实现**（点「保存设置」把 key/model/apiFormat 写回当前激活预设），本地 commit `45775d2`，未测试未推 |
| UA 伪装 | 全局写死 `Cline/3.0.0`（见「关键要点 3」） |
| 版本号 | 开发测试期不升，等发版才升（见「协作约定」） |

> 本地已 commit 但**均未推 GitHub、未经浏览器实测**：自定义下拉（`0294472`）、自动重试（`76bf658`）、模型可选（`78fcca7`）。待用户实测通过后再统一升版本号 + 推送。

## 三、关键文件

| 文件 | 说明 |
|---|---|
| `linux.sb-ai-reply.user.js` | 主脚本（唯一核心代码，约 1400+ 行） |
| `README.md` | 对外说明 |
| `.env` | **私密配置**（API key、cookie），**已加入 .gitignore，绝不入库** |
| `.workbuddy/memory/2026-*.md` | 工作日志（每天一篇，记录技术结论和踩坑） |
| `水贴专用-更新日志-v2.6*.md` | 发帖用的更新说明 |

## 四、主脚本代码结构（按模块）

脚本是一个 IIFE，大致顺序：

1. **元信息 + 常量**：`@version`、`DEFAULTS`（默认配置）、`DEFAULT_SYSTEM_PROMPT`（水贴提示词）、`DEFAULT_REPLY_SYSTEM_PROMPT`（水评论提示词）
2. **样式 CSS**：注入的面板样式（`CSS` 常量）
3. **配置读写**：`gmGet/gmSet`（GM_setValue 封装）、`loadConfig/saveConfig`、`loadProfiles/saveProfiles`（预设）
4. **抓取模块**：`getPosts` / `findFirstPost` / `getAuthorInfo` / `isOwnerPost`（按 UID 判断楼主）、`collectImages`（收图）、`cleanNode`、`extractMarkdown`（含 `<a>` 外链保留）、`scrapePosts` / `scrapeReplyTarget`（回应式抓取，追溯 @ 对话链）
5. **@解析与对话链**：`parseMentions` / `buildReplyChain`（递归追溯，Map 去重防死循环）
6. **AI 调用模块**：`buildRequest`（三格式构造请求，含 `CLIENT_UA`）、`sendRequest`、`requestAI`（单次调用）、`searchTools`、`parseResponses` / `parseChat` / `parseAnthropicJson` / `parseAnthropicSse`、`parsePairs`（解析 `{kw,fallback}` 关键词对）、`agentSearchReply`（四阶段联网搜索）
7. **UI 面板**：`buildPanel`、`makeDraggable`、`refreshProfileSelect` / `switchProfile` / `saveCurrentAsProfile` / `renameCurrentProfile` / `deleteCurrentProfile`（中转站预设）
8. **主流程**：`onGenerate` → `doGeneratePost`（总结式）/ `doGenerateReply`（回应式）、`onFill`（填入编辑器 + 抽奖算术验证）
9. **初始化**：`init` / `handleRouteChange`（SPA 无刷新导航适配）、MutationObserver 补注入「水它」按钮

## 五、待办任务（交给你的）

**1. 自定义下拉组件（预设管理 UI 改造）** ✅ 已完成（commit `0294472`，待浏览器实测）

已把原生 `<select>` 改成自定义下拉组件：触发器显示当前激活预设名 + `▾`，展开菜单每个预设条目右侧带 `✎重命名` / `✕删除`（删除加了 confirm 二次确认）。逻辑函数全保留，`renameCurrentProfile`/`deleteCurrentProfile` 改为接收 index 参数；事件用委托绑在菜单容器（refresh 重建 innerHTML 也不掉监听）；点组件外部关菜单。相关：`buildPanel` 预设 HTML 区块、`.lsb-ai-profile-dd*` CSS、`refreshProfileSelect`。
→ **待你做**：装到浏览器实测（展开/切换/重命名/删除/刷新识别激活项/外部点击关闭），OK 后升版本号 + 推 GitHub。

**2.（可选，未排期）图片方案**：帖子里图片目前是 URL 直传代理，会踩 5MB 超限 / 图床 403 / DNS 失败。方案是浏览器内下载转 base64 + 检查大小（>4.5MB 跳过）+ Content-Type 过滤。用户之前搁置了。

## 六、私密配置（.env 用法）

`.env` 文件在项目根目录，包含 3 个中转站的 key + linux.sb cookie。**测试 API 或抓帖时，从这里读，不要硬编码进代码，也不要写进 memory 或 README。**

```bash
# 例：测试 tabitoken 连通
curl -s https://tabitoken.com/v1/models \
  -H "Authorization: Bearer $TABITOKEN_API_KEY" \
  -H "User-Agent: Mozilla/5.0 ... Chrome/151 ..."   # tabitoken 有 Cloudflare，要浏览器 UA
```

- 3 个中转站特性见「关键要点」。
- `LINUX_SB_COOKIE` 会过期，失效后需登录 linux.sb 在 F12 重新取 `bbs_auth` / `bbs_csrf`。

## 七、关键要点与踩坑（务必先看）

1. **三格式 API**：脚本支持 `chat/completions`、`responses`、`anthropic(/messages)` 三种格式，通过配置的 `apiFormat` 切换。
2. **联网搜索四阶段**：规划(提炼 `{kw,fallback}` 关键词对) → parsePairs 解析 → 分批并行双搜(每批 3 次) → 汇总。**根因**：tabitoken 的 claude 调 `web_search` 时 `query` 恒等于整个 user content，所以搜索子代理的 content 必须是「纯关键词」，否则搜不到。
3. **UA 白名单**：agentrouter 有 UA 白名单闸门（只校验 UA、无签名），实测 **`Cline/3.0.0` 前缀可用**；`Kilo Code/*`、`claude-code/*`、`Roo-Code/*` 等都不行。当前脚本把 `CLIENT_UA = 'Cline/3.0.0'` **全局写死**，副作用是 tabitoken 的 Cloudflare 可能 403（它认浏览器 UA）。用户已知情，暂不改。
4. **拼写纠错**：阶段1 输出 `{kw, fallback}` 双字段（kw 精确词 + fallback 泛化词），阶段3 双搜，阶段4 加「结合上下文判断正确写法」的通用纠错指导。解决了帖子写错产品名（如 Grok Harvey → SuperGrok Heavy）时 AI 跟着写错的问题。
5. **多义词边界**：纠错不能机械替换，要结合上下文（用户明确提过：某词在不同领域含义不同，机械替换会跑偏）。
6. **推送 GitHub**：本机 git 的 SSL 栈有问题（schannel 吊销检查 / openssl 缺 CA），`git clone/push` 会失败。**改用 `gh api` 直传文件**：先 `gh api .../contents/<file> --jq .sha` 取当前 sha，再用 `gh api -X PUT .../contents/<file>` 上传（`content` 字段 base64）。**注意：推送前必须现取 sha，否则 409**（远程 sha 会变）。
7. **提示词不要过拟合**：改提示词要保持通用，不要为某个具体帖子/案例特化（用户明确要求过）。
8. **请求自动重试**：`sendRequest` 已内建重试（拆成 `sendRequestOnce` + 重试循环）。只对可重试失败重试——网络错误 / 超时 / 状态码 `[408,429,500,502,503,504,529]`；401/403/400、用户取消、解析失败不重试。`MAX_RETRY=2`、退避 1s→2s。加在最底层，故各调用独立重试（阶段3 并行子搜索单个失败只重试自己，不重跑全流程）。⚠️ 超时(180s)也重试，最坏 3×180s。
9. **agentrouter 联网**：支持（responses + `web_search` 工具，UA `Cline/3.0.0`，GPT 原生 web_search 会自己拆 query）。若报 503「No upstream account available」是**它家上游账号池临时空了**（间歇性、服务端问题、跟脚本无关），重试/换中转站即可——这也是加自动重试的直接动因。
10. **模型可选**：模型框套自定义筛选下拉 `.lsb-ai-model-dd`（input + ▾ + 含独立筛选框的菜单）。点「拉取」走 `GET {baseUrl}/models`（返回 `{data:[{id}]}`），去重排序存内存 `modelOptions` 并渲染下拉，同时缓存进 baseUrl 匹配的预设 `p.models`（切回该预设自动回填、免重拉）。点 ▾ 展开、**上方独立小框筛选**（不碰主输入框，避免功能重合）、点条目写入主框并 saveConfig 生效；主框仍可纯手输。选模型不自动写回预设（要设默认得「存为当前预设」）。预设对象多了可选字段 `models: string[]`。（注：早期用过原生 datalist，因无箭头 + 按主框值过滤只剩一条，已弃用改自定义下拉。）

## 八、协作约定（用户偏好，务必遵守）

1. **先对齐方案再动代码**：改代码前先把方案讲给用户确认，用户说「可以/动手」才写。
2. **版本号策略**：开发测试期不动版本号；正式发版才升——新功能 minor（x.1.0）、修 bug patch（x.x.1，攒批）、大重构 major。不要每个小改动都升版本。
3. **每改完 commit 本地 git**（方便回退），但「推 GitHub」要等用户明确说。
4. **沟通风格**：中文、口语化、低 AI 味、别官腔；用户是实战派，喜欢直接、结构化、带表格/命令的说明。
5. 涉及个人文件/私密信息的操作要谨慎，key 不外泄、不写进会被提交的文件。
