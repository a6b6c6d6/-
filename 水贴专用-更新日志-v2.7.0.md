# 【水贴专用】v2.7.0：多中转站切换 + 模型可选 + 自动重试

这版主要是把「多中转站 + 多模型」用起来更顺手，另外加了请求失败自动重试，少一点报错干瞪眼的情况。

## 这次更新了啥

**1. 中转站预设管理**
面板顶部的预设列表改成了自定义下拉，每一条右边直接带「✎重命名」「✕删除」（删除会二次确认，防误点）。多个中转站（tabitoken / relaylink / agentrouter 之类）一点就切，切过去 key、模型、接口格式一起带过来，立即生效。

**2. 模型可以选了，不用手打**
一个中转站几十上百个模型，以前只能手敲、或者为了换模型多存好几条预设。现在：
- 模型框右边有个「拉取」按钮，点一下把这个站的模型列表全拉下来；
- 再点模型框旁边的 ▾ 展开，上面有个独立的筛选小框，打字就能过滤，点一下选中；
- 拉过的列表会按预设缓存，下次切回这个站不用重拉；
- 想手打没列出来的模型也照样行。

**3. 「保存设置」能同步回当前预设**
正在用某个预设，临时改了模型或 key，点最下面「保存设置」就会把改动写回这个预设——不用再「存为当前预设」新建一条了。（只改模型/key 的场景；要是连接口地址都换了，那算切到别的站，不会误改原预设。）

**4. 请求失败自动重试**
中转站上游偶尔会抽风（比如报 503「No upstream account available」，那是它家账号池那一刻空了，跟你没关系）、或者网络抖一下。现在这类临时失败会自动重试（最多 2 次，间隔 1 秒、2 秒），不用自己手动重来。只对临时性错误重试；key 错、参数错这种不会瞎重试。

## 更新方式

- 点这个链接装/更新：[水贴专用.user.js](https://raw.githubusercontent.com/a6b6c6d6/lzsb-smart-water-sticker/main/linux.sb-ai-reply.user.js)
- 已经装过的，Tampermonkey 里「检查更新」也能收到。
